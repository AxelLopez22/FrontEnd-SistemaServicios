import { LimitarDigitosDirective } from './limitar-digitos.directive';

describe('LimitarDigitosDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitarDigitosDirective();
    expect(directive).toBeTruthy();
  });
});
